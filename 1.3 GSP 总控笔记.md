---
最后更新:
  - 2026-09-01 11:35:08
---

# 1.3 GSP

> Grok Studio Pro 旧项目翻新总进度与下一步规划

## 当前阶段

界面重做和体验收口这一轮已经做完：八条渠道进了统一适配层，五个主要页面按设计稿重做，工程门禁（tsc / lint / 单测 / build）全部通过，并补上了 89 个 Playwright e2e。缺的仍然是同一件事——真实渠道验收。所有测试都用模拟响应，没有一次真实生成被记录过，所以现在能说的是"代码路径成立"，不是"渠道可用"。下一阶段是把 24 个提交收口进 `main`，然后拿真实凭证按清单跑一遍。

- 当前定位：浏览器端 AI 图片/视频工作台，长期方向是聚合免费渠道和自有号池。
- 当前版本：尚未建立独立产品版本号。
- 项目路径：`D:\8.Project\0_0_维护\grok-studio-pro`
- 当前分支：`codex/provider-experience-redesign`
- Git 状态：当前分支比 `origin/main` 领先 24 个提交、落后 0 个；本地 `main` 与 `origin/main` 一致（上一版笔记说本地 `main` 领先 9 个，已不成立）。
- 发布状态：24 个提交都没有合并到 `main`，也没有推送到 GitHub。
- 自动化验证：2026-09-01 全绿。`npx tsc --noEmit` 0 错误；`npx eslint .` 0 错误 / 8 个 Fast Refresh 警告；单测 265/265；e2e 89/89；`npm run build` 成功。
- 真实渠道验收：**仍然是零**。没有任何一条渠道完成过真实生成记录。
- 当前资源：已经没有 xAI API；图生图、视频、漫画上色和翻译都只在 xAI 上有实现，因此这四块目前无法实测。

## 下次打开项目先做

1. 审这 24 个提交并合进 `main`，再决定推送方式。
2. 用真实 Gitee AI Token（每日 100 张，八条渠道里门槛最低）跑一次文生图，把"真实渠道验收"从零变成一。
3. 移动端过一遍设置页和画廊：两处 L2 nav 都是固定 `w-[208px]`，没有断点，窄屏必然挤。

## 总览

| 分区     | 子项                  | 当前状态                  | 下一步 / 备注                                                    |
| -------- | --------------------- | ------------------------- | ---------------------------------------------------------------- |
| 架构     | 图片 Provider 适配层  | 已完成                    | 统一接口、注册表、能力检查；号池调度放在注册表上层               |
| 架构     | 插件化骨架            | 骨架完成，未接 UI         | 注册/注销、生命周期、配置 schema、事件总线、图片来源钩子都在     |
| 兼容     | xAI / NewAPI          | 代码保留，当前不可实测    | 没有可用 xAI API；图生图、视频、漫画都只在这条渠道上有实现       |
| 免费渠道 | Gitee AI 模力方舟     | 已接入，待真实验收        | 每日 100 张，门槛最低，应作为第一个验收目标                      |
| 免费渠道 | ModelScope            | 已接入，待真实验收        | 需要 Token；单模型约 50 张/天                                    |
| 免费渠道 | AI Horde              | 已接入，待真实验收        | 可匿名；新号 Kudos 为 0，只能出约 711×711 以下                   |
| 免费渠道 | Pollinations          | 已接入，待真实验收        | 按 Pollen 计费且不每日刷新，不能记为无限免费                     |
| 号池     | PixAI 官方 API        | 已接入，待真实验收        | 普通用户需申请审核，公开免费额度未知                             |
| 号池     | PixAI 网页 Token      | 已接入，实验性            | Token 约一周过期；协议可能变化                                   |
| 号池     | PixAI 号池            | 仅登记和阻断提示          | 缺真实 HAR，当前不会发送猜测请求                                 |
| 调度     | 额度计数与降级        | 已接入文生图              | 本地计数用尽时自动换渠道并提示；同人图批量仍固定单渠道           |
| 调度     | 自动轮换 / 健康度     | 未开发                    | 等至少两条渠道真实跑通后再设计                                   |
| 文生图   | 单张和批量生成        | 已按设计稿重做            | 分辨率超上限的档位置灰；额度徽标显示今日剩余                     |
| 图生图   | 编辑与多图融合        | xAI 专用                  | 暂无可用渠道                                                     |
| 视频     | 生成、编辑、延长      | xAI 专用，页面已重做      | 有任务追踪和按秒计费预估；当前资源下不可实测                     |
| 同人图   | 场景批量生成          | 已按设计稿重做            | 单项重试和"重试全部失败项"已加；重试复用提交时记下的提示词       |
| 漫画     | 上色和翻译            | xAI 专用，页面已重做      | 批处理队列 + 连续阅读器；支持 ZIP/CBZ 和文件夹导入，上限 200 页  |
| 画廊     | IndexedDB、筛选和下载 | 已按设计稿重做            | 左 L2 nav 分面筛选；真实浏览器仍需验证保存失败和批量下载         |
| 文档     | README 使用说明       | 已完成                    | 已记录渠道限制、并发、CORS、本地凭证和常见错误                   |
| Windows  | 启动与测试脚本        | 已完成                    | `启动.bat`、`测试.bat` 已加入 CRLF 回归测试                      |
| 工程     | 单元测试              | 265/265 通过              | Node 内置 runner，模拟响应为主                                   |
| 工程     | e2e 测试              | 89/89 通过                | Playwright，7 个 spec；全部拦截网络，不打真实渠道                |
| 工程     | ESLint / Prettier     | 0 错误 / 8 警告           | 上一版笔记说的 CRLF 阻断已在 e782931 修掉                        |
| 工程     | 生产构建              | 成功                      | `npm run build`                                                  |
| 发布     | 分支整合和推送        | 待处理                    | 24 个提交尚未进入远端                                            |

## 已完成内容

按提交顺序，只列这一轮（24 个提交）里做完的事：

- 修复 Windows 下 lint 闸门失效，补齐 xAI 专属功能的渠道校验。
- 轮询改为先查询再等待；漫画批量任务支持取消。
- 删除 huggingface 渠道，新增 Gitee AI 模力方舟。
- 各渠道模型目录与分辨率档位；设置页模型选择挪到生成页。
- 接入 Playwright e2e，并修掉它揪出的三个渲染 bug 和约 1/3 概率的抖动。
- 画廊、设置页、文生图、同人图、漫画、视频六个页面按设计稿重做，设置页和画廊改为左 L2 nav。
- 修掉三处水合不匹配（同人图）和五处排版问题（侧边栏、画廊封面、设置页）。
- 漫画工具：批处理队列 + 连续阅读器，替掉只能看一页的预览弹窗。
- 视频页：任务追踪与按秒计费预估。
- 分辨率档位按渠道和模型上限置灰，避免用户选一个会被静默降低的值。
- 同人图批量：单项重试 + 重试全部失败项。
- 额度计数接入 UI：设置页显示今日已用/上限，文生图显示今日剩余，用尽时自动换渠道。
- 漫画工具支持 ZIP/CBZ 和文件夹导入，页数上限 30 → 200。
- 插件化架构骨架（类型、注册表、空示例、15 个单测）。

## 当前限制和已知问题

### 1. 真实渠道验收仍然为零

265 个单测和 89 个 e2e 全部使用模拟响应或拦截网络——e2e 里的凭证是 `e2e-fake-*`，任何一条会真实发请求的路径都被 `page.route` 截住了。这是有意的（否则断言会全变成关于错误提示的断言），但它的代价是：测试全绿不能推出任何一条渠道可用。真实服务仍可能受 Key、额度、模型下线、CORS、Cookie 和限频影响。

### 2. Git 尚未收口

24 个提交都在 `codex/provider-experience-redesign`，没有合并也没有推送。本地 `main` 这次与 `origin/main` 一致，所以整合范围就是这 24 个。

### 3. 额度计数是本地估算，不是渠道余量

`src/lib/quota.ts` 记的是"这个浏览器通过本工具发出过多少次成功请求"，按渠道按天存在 localStorage。它不是渠道返回的余量，因此：换浏览器或清缓存会归零、在别处用掉的额度它不知道、渠道口径与本地计数不一致时以渠道为准。UI 文案已经明说这点，不要在别处把它当成权威余量。

### 4. 漫画页以 data URI 存在内存里

页面存在 zustand store 里，data URI 比原图大约 1.4 倍，200 张大扫图约 300-400MB，且刷新即失（store 不持久化）。上限 200 是内存决定的，不是常数决定的。升级路径是改用 object URL 承载导入器已经产出的 blob，能去掉 base64 开销并让浏览器换页，但那是比改常数大得多的改动。

### 5. 置灰档位没有真正的 tooltip

Radix 的 `SelectItem` 被禁用后会吞掉指针事件，原生 `title` 因此不会触发。上限信息改为写在选项行内的 hint 和控件下方的说明里（"当前模型最高支持 XXXpx"），信息到位，但不是悬浮提示。

### 6. 同人图批量不参与渠道降级

文生图会在本地计数用尽时自动换渠道，同人图批量刻意没有接：批量中途换渠道后，队列必须逐项显示各自用的是哪条渠道，否则结果来源无法追溯。等队列支持按项显示渠道后再接。

### 7. 插件骨架还没有接入口

`src/lib/plugins/` 只有类型、注册表和一个空示例，`pluginRegistry` 是空的，`readConfig` 没有接 localStorage，设置页也还没有渲染插件配置表单。这是有意的——没有具体插件之前，写表单渲染就是写死代码。

### 8. 移动端未适配

设置页和画廊的两处 L2 nav 都是固定 `w-[208px]`，没有断点。窄屏下必然挤压主栏。

### 9. PixAI 号池缺少协议证据

`imgapi.qianyimwl.top` 已登记，但没有一次成功生成的 HAR。需要从自己有权使用的网页会话中确认提交、轮询、取图和登录态，不猜接口，也不绕过网站访问控制。

### 10. 还有 8 个 Fast Refresh 警告

都是"文件同时导出组件和非组件"，只影响开发时热更新粒度，不影响构建。要么拆文件，要么明确豁免。

## 真实渠道验收顺序

### 1. 工程环境

- [x] `npx tsc --noEmit` 0 错误
- [x] `npx eslint .` 0 错误（8 个 Fast Refresh 警告）
- [x] 单测 265/265
- [x] e2e 89/89
- [x] `npm run build` 成功
- [ ] 双击 `启动.bat`，确认页面可以正常打开
- [ ] 移动端检查设置页和画廊的 L2 nav 没有溢出或遮挡
- [ ] 处理或明确豁免 8 个 Fast Refresh 警告

### 2. Gitee AI 模力方舟（第一优先）

- [ ] 用真实 Token 生成 1 张图片
- [ ] 确认结果带正确渠道名并写入画廊
- [ ] 确认额度徽标的今日剩余随生成递减
- [ ] 验证超出模型上限的分辨率档位确实置灰，且选中档位不会被静默降低
- [ ] 记录真实每日额度刷新时间和限频表现

### 3. AI Horde

- [ ] 不填 Key，以匿名身份生成 1 张图片
- [ ] 确认排队期间状态可见，长队列不会被当成程序卡死
- [ ] 取消任务后停止轮询，不继续写入画廊
- [ ] 验证失败任务、超时和无图片结果的提示
- [ ] 确认新号 Kudos 为 0 时的低分辨率限制与 UI 提示一致
- [ ] 确认没有额度上限的渠道不显示今日额度徽标

### 4. Pollinations 与 ModelScope

- [ ] Pollinations：用有效 Key 生成 1 张，验证 401 / 402 / 429 的原始错误信息
- [ ] Pollinations：记录真实 Pollen 消耗，不使用"永久免费"表述
- [ ] ModelScope：用有效 Token 完成一次异步生成，验证失败、取消和超时路径
- [ ] 两条渠道都确认多图请求串行执行

### 5. 额度降级

- [ ] 把某渠道今日上限调到 1，生成 2 次，确认第 2 次自动换渠道并给出提示
- [ ] 确认清零立即生效且不脏化设置草稿
- [ ] 确认清空上限视为不限，而不是存成 0

### 6. 页面和画廊

- [ ] 每个已启用渠道都能在设置页保存并重新加载配置
- [ ] 下载单图、批量 ZIP、删除和筛选正常
- [ ] 页面切换后输入和任务状态仍然保留
- [ ] 不支持图生图或视频的渠道显示明确提示，不发送错误请求
- [ ] 浏览器存储失败时界面给出可见错误

### 7. 漫画导入（不需要渠道）

- [ ] 拖入一个真实 CBZ，确认页序与阅读顺序一致
- [ ] 选择一个分章节的文件夹，确认章节不交错
- [ ] 导入一个 200 页以上的卷，确认提示说清总页数和实际导入数
- [ ] 观察 200 页时的内存占用，判断上限是否需要下调

## PixAI 接入清单

- [ ] 打开 DevTools Network，清空旧请求后成功生成 1 张图片
- [ ] 导出已打码的 HAR，保留 URL、方法、必要请求头、请求体和响应结构
- [ ] 找到提交任务接口及任务 ID 字段
- [ ] 找到轮询接口、状态机和取图字段
- [ ] 确认登录态载体（Cookie / Token / 签名）及有效期
- [ ] 写适配器并补单测，并发固定为 1 做真实验证

## 里程碑

| 里程碑          | 判定标准                                                    | 状态           |
| --------------- | ----------------------------------------------------------- | -------------- |
| M0 翻新基础     | 可靠性修复、Provider 注册表、渠道边界、文档和脚本提交       | 已完成         |
| M1 工程收口     | tsc / lint / 单测 / e2e / build 全绿                        | 已完成         |
| M2 分支收口     | 24 个提交进入 `main` 并推送                                 | 未完成         |
| M3 免费渠道可用 | 至少两条非 xAI 渠道完成真实生成和画廊保存                   | 未完成         |
| M4 PixAI 接入   | 协议有证据、适配器有测试、并发 1 真实生成通过               | 未完成         |
| M5 号池可用     | 多账号冷却、轮换、错误隔离和任务归属可追踪                  | 未完成         |
| M6 聚合测试版   | 多渠道回退可控，主要页面完成真实浏览器验收                  | 未完成         |

## 长期延期事项

- 为免费渠道寻找可靠的图生图和多图融合接口。
- 寻找不依赖 xAI 的视频生成、编辑和延长渠道。
- 漫画翻译改用可替换的多模态模型和图片编辑 Provider。
- 漫画页改用 object URL 承载，去掉 data URI 的 base64 开销。
- 插件配置表单的自动渲染，以及 `readConfig` 接 localStorage。
- 本地生成后端（ComfyUI / SD WebUI）作为第九条渠道，摆脱远端额度限制。
- 动态 Provider 发现、复杂评分、成本优化和配额预测。
- 自建代理或后端托管凭证，降低浏览器直连的 CORS 和安全限制。
- 真实渠道的定时冒烟测试、协议变化检测和告警。
- 画廊迁移、导入导出和跨浏览器同步。
- 移动端适配。
- 正式版本、变更日志、部署和发布流程。

## 更新规则

- 每次只写已经发生的事实；Mock 测试通过不等于真实渠道通过。
- 完成真实生成后再勾选渠道验收项，并记录日期、账号类型、模型和并发数。
- `当前阶段` 保持一段话，`下次打开项目先做` 最多保留三项。
- 新问题放入对应验收清单；暂时不做且不阻断当前里程碑的事项移到长期延期。
- 每次合并、推送或切换主线后，立即更新分支、提交差异和验证结果。
